type Client {
    id: ID!,
    name: String,
    email: String
}

extend type Query {
    client(id: Int): Client,
    clients: [Client]
}

extend type Mutation {
    saveClient(id: ID, name: String, email: String): Client
    deleteClient(id: ID): Boolean
}